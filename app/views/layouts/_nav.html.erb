<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Hacker news</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/">Home</a>
        </li>
      </ul>
      <div class="d-flex">
        <% unless user_signed_in? %>
          <%= link_to("登入", new_user_session_path, class: 'btn btn-primary me-1') %>
          <%= link_to("註冊", new_user_registration_path, class: 'btn btn-warning') %>
        <% else %>
          <%= link_to("發文", new_post_path, class: 'btn btn-primary me-1') %>
          <%= button_to("登出", destroy_user_session_path, method: :delete, class: 'btn btn-danger') %>
        <% end %>
      </div>
    </div>
  </div>
</nav>